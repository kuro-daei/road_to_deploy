# プログラミングの基礎知識

## 1. プログラミングとは

プログラミングとは、コンピュータに対して実行させたい処理を「プログラム」という形で記述する作業です。  
プログラムは**プログラミング言語**を使って書かれ、コンピュータはその指示に従って動作します。

---

## 2. 変数とデータ型

### 変数
変数とは、データを一時的に保存するための「箱」のようなものです。

```python
# Python の例
name = "Alice"
age = 25
```

### 主なデータ型

| データ型 | 説明 | 例 |
|----------|------|----|
| 整数 (int) | 整数値 | `42`, `-10` |
| 浮動小数点数 (float) | 小数を含む数値 | `3.14`, `-0.5` |
| 文字列 (string) | テキストデータ | `"Hello"`, `"世界"` |
| 真偽値 (boolean) | 真 (true) または偽 (false) | `true`, `false` |
| 配列 (array) | 複数の値をまとめたもの | `[1, 2, 3]` |

---

## 3. 制御構造

### 条件分岐 (if文)
条件に応じて処理を分けます。

```python
# Python の例
score = 80

if score >= 90:
    print("優")
elif score >= 70:
    print("良")
else:
    print("可")
```

### ループ (繰り返し処理)
同じ処理を繰り返します。

```python
# for ループ
for number in range(5):
    print(number)  # 0, 1, 2, 3, 4 を出力

# while ループ
count = 0
while count < 3:
    print(count)  # 0, 1, 2 を出力
    count += 1
```

---

## 4. 関数

関数とは、特定の処理をひとまとめにして、名前をつけて再利用できるようにしたものです。

```python
# Python の例
def greet(name):
    return f"こんにちは、{name}さん！"

message = greet("太郎")
print(message)  # こんにちは、太郎さん！
```

### 関数のメリット
- コードの**再利用**ができる
- プログラムを**読みやすく**できる
- 修正箇所を**一箇所にまとめられる**

---

## 5. オブジェクト指向プログラミング (OOP)

オブジェクト指向とは、データと処理をひとまとめにした「オブジェクト」を基本単位としてプログラムを設計する考え方です。

### クラスとインスタンス

```python
# Python の例
class Dog:
    def __init__(self, name):
        self.name = name

    def bark(self):
        print(f"{self.name}：ワン！")

my_dog = Dog("ポチ")
my_dog.bark()  # ポチ：ワン！
```

### OOPの主な概念

| 概念 | 説明 |
|------|------|
| カプセル化 | データと処理をひとまとめにして、外部から直接操作されないよう保護する |
| 継承 | 既存のクラスの機能を引き継いで新しいクラスを作る |
| ポリモーフィズム | 同じ操作でも、オブジェクトの種類によって異なる動作をする |

---

## 6. アルゴリズムとデータ構造

### アルゴリズム
問題を解くための「手順・方法」のことです。

**代表的なアルゴリズム：**
- **ソート（並べ替え）**：バブルソート、クイックソートなど
- **探索**：線形探索、二分探索など

### データ構造
データを効率的に扱うための「データの整理方法」です。

| データ構造 | 特徴 |
|------------|------|
| 配列 (Array) | インデックスで高速にアクセス可能 |
| リスト (List) | 要素の追加・削除が容易 |
| スタック (Stack) | 後入れ先出し (LIFO) |
| キュー (Queue) | 先入れ先出し (FIFO) |
| 辞書 / ハッシュ (Dictionary) | キーと値のペアで管理 |

---

## 7. バージョン管理 (Git)

Gitはソースコードの変更履歴を管理するためのツールです。チームでの開発に欠かせません。

### 基本的なGitコマンド

```bash
# リポジトリの初期化
git init

# ファイルをステージングエリアに追加
git add .

# 変更をコミット（保存）
git commit -m "コミットメッセージ"

# リモートリポジトリに変更を送信
git push origin main

# リモートリポジトリから最新の変更を取得
git pull origin main
```

---

## 8. デバッグとエラー処理

### デバッグ
バグ（不具合）を見つけて修正する作業です。

**デバッグの方法：**
1. `print` 文などでデータの内容を確認する
2. デバッガーツールを使ってステップ実行する
3. エラーメッセージをよく読む

### エラー処理 (例外処理)

```python
# Python の例
try:
    result = 10 / 0
except ZeroDivisionError:
    print("0で割ることはできません")
finally:
    print("処理が完了しました")
```

---

## 9. よく使われるプログラミング言語

| 言語 | 用途 |
|------|------|
| Python | AI/機械学習、データ分析、Web開発 |
| JavaScript | Web フロントエンド・バックエンド |
| Java | エンタープライズシステム、Android アプリ |
| Ruby | Web開発（Ruby on Rails） |
| Go | バックエンドサービス、クラウドインフラ |
| Rust | システムプログラミング、高パフォーマンスが求められる分野 |

---

## 10. 次のステップ

プログラミングの基礎を学んだら、以下のステップに進みましょう：

1. **Webの基礎**：HTML / CSS / HTTP の理解
2. **データベース**：SQL の基礎
3. **フレームワーク**：使いたい言語のフレームワークを学ぶ
4. **インフラ・デプロイ**：サーバー、クラウド、CI/CD の理解

> 「プロダクトをデプロイするまでの長い道のり」の第一歩はプログラミングの基礎から始まります！
